rules_version = '2';
// TEST MODE: all reads and writes are open. Tighten rules before going to production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
